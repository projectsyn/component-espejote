parameters:
  espejote:
    resources:
      espejote:
        limits:
          cpu: 200m
          memory: 256Mi
      rbac_proxy:
        limits:
          cpu: 1
          memory: 256Mi

    jsonnetLibraries:
      my-lib:
        spec:
          foo: bar

    managedResources:
      without-sa:
        spec:
          foo: bar
      with-sa:
        spec:
          foo: bar
          serviceAccountRef:
            name: sa-with
      with-sa-in-spec:
        spec:
          foo: bar
          serviceAccountRef:
            name: sa-in-spec
      my-namespace/copy-configmap:
        _clusterRules:
          namespace:
            apiGroups:
              - ""
            resources:
              - namespace
            verbs:
              - list
        _rules:
          configmap:
            apiGroups:
              - ""
            resources:
              - configmap
            verbs:
              - list
              - get
              - create
        spec:
          foo: bar
      my-namespace/copy-secret:
        _clusterRoles:
          - cluster-admin
        _roles:
          - admin
          - argocd-manager
        spec:
          foo: bar
          serviceAccountRef:
            name: copy-configmap
